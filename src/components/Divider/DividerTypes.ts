export enum DividerType {
  line = 'line',
  filled = 'filled',
}

export enum DividerOrientation {
  horizontal = 'horizontal',
  vertical = 'vertical',
}

export enum DividerSizes {
  fullWidth = 'fullWidth',
  inset = 'inset',
  middle = 'middle',
}
